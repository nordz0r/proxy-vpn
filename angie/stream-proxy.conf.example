# Stream-проброс для HTTPS-прокси
# Копировать в /etc/angie/stream.d/proxy.example.com.conf
#
# ВАЖНО: Убедитесь, что в /etc/angie/angie.conf есть блок:
#   stream {
#       include /etc/angie/stream.d/*.conf;
#   }
#
# Клиент подключается к proxy.example.com:444 как HTTPS-прокси.
# Angie терминирует TLS и пробрасывает TCP в Xray.
#
# Настройка в браузере/системе:
#   HTTPS proxy: proxy.example.com:444
#
# Настройка в curl:
#   curl --proxy https://proxy.example.com:444 https://ifconfig.me

# HTTP proxy over TLS
server {
    listen 444 ssl;

    ssl_certificate     /etc/letsencrypt/live/example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;

    proxy_pass 127.0.0.1:3128;
}

# SOCKS5 over TLS (опционально)
# server {
#    listen 445 ssl;

#    ssl_certificate     /etc/letsencrypt/live/example.com/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;

#    proxy_pass 127.0.0.1:1080;
#}
